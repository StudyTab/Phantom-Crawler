# PhantomCrawler Dependencies
# Install with: pip3 install -r requirements.txt

# HTTP requests library
requests>=2.31.0

# HTML/XML parsing
beautifulsoup4>=4.12.0

# Terminal colors and formatting
colorama>=0.4.6

# HTML parser for BeautifulSoup (optional but recommended)
lxml>=4.9.0

# SSL/TLS support (usually included with Python)
certifi>=2023.7.22

# URL parsing and handling (built-in, but explicit version for compatibility)
urllib3>=2.0.0